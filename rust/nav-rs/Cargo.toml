[package]
name = "nav-rs"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Experimental Rust core for Navi8or"
publish = false

[dependencies]
crossterm = { version = "0.29.0", default-features = false, features = ["events", "windows"] }

