--- a/termbox2.h
+++ b/termbox2.h
@@ -3480,7 +3480,7 @@
 
     do {
         int pending_alt_escape =
-            timeout < 0 && (global.input_mode & TB_INPUT_ALT) &&
+            timeout != 0 && (global.input_mode & TB_INPUT_ALT) &&
             global.in.len == 1 && global.in.buf[0] == '\x1b';
 
         FD_ZERO(&fds);
